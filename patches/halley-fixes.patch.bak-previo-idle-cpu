diff --git a/crates/halley-wl/src/backend/tty/drm.rs b/crates/halley-wl/src/backend/tty/drm.rs
index 86584c1..6ee3ed5 100644
--- a/crates/halley-wl/src/backend/tty/drm.rs
+++ b/crates/halley-wl/src/backend/tty/drm.rs
@@ -1352,6 +1352,7 @@ pub(crate) fn queue_tty_drm_frame(
                     local_cursor,
                     cursor_image,
                     &st.runtime.tuning.cursor,
+                    &mut st.platform.cursor_manager,
                     Kind::Unspecified,
                 )?;
                 match compositor.render_frame(
@@ -1425,15 +1426,12 @@ pub(crate) fn queue_tty_drm_frame(
             }
         }
 
-        let fullscreen_needs_paced_frames =
-            crate::frame_loop::output_has_pending_frame_callbacks(st, output_name)
-                && st
-                    .model
-                    .fullscreen_state
-                    .fullscreen_active_node
-                    .contains_key(output_name);
+        // El path de direct-scanout ya pacea por page-flip + presentation
+        // feedback (mas abajo), asi que NO hay que desactivarlo por tener
+        // frame-callbacks pendientes. Un juego fullscreen SIEMPRE tiene
+        // callbacks pendientes, y el antiguo gate hacia oscilar scanout<->GL
+        // cada frame -> parpadeo y ~10 fps menos e inestables en la HD530.
         let allow_direct_scanout = !disable_direct_scanout
-            && !fullscreen_needs_paced_frames
             && primary_render_node == output_render_node;
         match allow_direct_scanout.then(|| {
             fullscreen_direct_scanout_candidate(
@@ -1475,6 +1473,7 @@ pub(crate) fn queue_tty_drm_frame(
                     local_cursor,
                     cursor_image,
                     &st.runtime.tuning.cursor,
+                    &mut st.platform.cursor_manager,
                 )?;
                 elements.extend(render_elements_from_surface_tree::<
                     _,
@@ -1675,6 +1674,7 @@ pub(crate) fn queue_tty_drm_frame(
                     local_cursor,
                     cursor_image,
                     &st.runtime.tuning.cursor,
+                    &mut st.platform.cursor_manager,
                 )?;
                 elements.extend(cursor_elements);
             }
@@ -1933,10 +1933,11 @@ fn render_tty_direct_elements(
     local_cursor: Option<(f32, f32)>,
     cursor_image: Option<&CursorImageStatus>,
     cursor_config: &halley_config::CursorConfig,
+    cursor_manager: &mut crate::render::CursorManager,
     surface_kind: Kind,
 ) -> Result<Vec<HalleyDirectScanoutElement>, Box<dyn Error>> {
     let mut elements =
-        direct_scanout_cursor_elements(renderer, local_cursor, cursor_image, cursor_config)?;
+        direct_scanout_cursor_elements(renderer, local_cursor, cursor_image, cursor_config, cursor_manager)?;
     elements.extend(render_elements_from_surface_tree::<
         _,
         HalleyDirectScanoutElement,
@@ -1949,6 +1950,7 @@ fn direct_scanout_cursor_elements(
     local_cursor: Option<(f32, f32)>,
     cursor_image: Option<&CursorImageStatus>,
     cursor_config: &halley_config::CursorConfig,
+    cursor_manager: &mut crate::render::CursorManager,
 ) -> Result<Vec<HalleyDirectScanoutElement>, Box<dyn Error>> {
     let Some((sx, sy)) = local_cursor else {
         return Ok(Vec::new());
@@ -1982,8 +1984,10 @@ fn direct_scanout_cursor_elements(
             )
         }
         CursorImageStatus::Named(icon) => {
-            let Some(sprite) =
-                crate::render::themed_cursor_sprite_with_fallback(cursor_config, icon)
+            // Sprite via el CursorManager (NO el cache global): así se prima el
+            // estado de animación (started_at/cycle_ms) también cuando el cursor
+            // sale por el plano HW de DRM, donde draw_cursor_layer no se ejecuta.
+            let Some(sprite) = cursor_manager.sprite_with_fallback(cursor_config, icon)
             else {
                 return Ok(Vec::new());
             };
@@ -1991,8 +1995,12 @@ fn direct_scanout_cursor_elements(
                 sx.round() as i32 - sprite.hotspot_x,
                 sy.round() as i32 - sprite.hotspot_y,
             );
+            // Avance de frames del cursor animado (XCursor multi-frame).
+            // elapsed se toma DESPUÉS de resolver el sprite (que primó la animación).
+            let elapsed_ms = cursor_manager.cursor_elapsed_ms();
+            let frame_pixels = &sprite.frame_at(elapsed_ms).pixels_bgra;
             let buffer = MemoryRenderBuffer::from_slice(
-                &sprite.pixels_bgra,
+                frame_pixels,
                 Fourcc::Argb8888,
                 (sprite.width as i32, sprite.height as i32),
                 1,
@@ -2176,7 +2184,19 @@ fn fullscreen_direct_scanout_candidate(
     if st.should_draw_focus_ring_preview(Instant::now()) {
         return Some(blocked("focus preview is active"));
     }
-    if monitor_has_blocking_layer_shell_surfaces(st, output_name) {
+    // Las capas Top/Overlay (barra Noctalia, notificaciones) se OCULTAN en
+    // fullscreen asentado via `suppress_top_overlay_layers`. Si aun asi
+    // bloquearan el direct-scanout, el juego caeria al path compuesto (GL)
+    // con la barra igualmente invisible = lento y sin motivo. Ya pasamos el
+    // gate de "transicion animando" arriba, asi que si este candidato es el
+    // nodo fullscreen activo del monitor, esta asentado -> capas suprimidas.
+    let is_settled_fullscreen_node = st
+        .model
+        .fullscreen_state
+        .fullscreen_active_node
+        .get(output_name)
+        == Some(&node_id);
+    if !is_settled_fullscreen_node && monitor_has_blocking_layer_shell_surfaces(st, output_name) {
         return Some(blocked(
             "top/overlay layer-shell surfaces are present on the output",
         ));
diff --git a/crates/halley-wl/src/compositor/fullscreen/system.rs b/crates/halley-wl/src/compositor/fullscreen/system.rs
index 712b7cd..1c04a2e 100644
--- a/crates/halley-wl/src/compositor/fullscreen/system.rs
+++ b/crates/halley-wl/src/compositor/fullscreen/system.rs
@@ -1882,6 +1882,20 @@ pub(crate) fn fullscreen_visual_animation_active_for_node_on_current_monitor_at(
         })
 }
 
+/// True when the current render monitor has a settled fullscreen window (game mode).
+///
+/// Used by the frame builder to suppress the Top/Overlay layer-shell surfaces (the
+/// Noctalia bar, notifications, launcher) so a fullscreen app covers them like every
+/// other WM does. Gated on the visual animation being idle so the bar re-appears
+/// while the fullscreen zooms out on exit instead of popping back only at the end.
+pub(crate) fn current_monitor_has_settled_fullscreen(st: &Halley, now: Instant) -> bool {
+    let monitor = st.model.monitor_state.current_monitor.as_str();
+    let Some(&node_id) = st.model.fullscreen_state.fullscreen_active_node.get(monitor) else {
+        return false;
+    };
+    !fullscreen_visual_animation_active_for_node_on_current_monitor_at(st, node_id, now)
+}
+
 pub(crate) fn is_fullscreen_session_node(st: &Halley, node_id: NodeId) -> bool {
     st.model
         .fullscreen_state
@@ -2039,7 +2053,20 @@ fn request_toplevel_fullscreen_state(
             }
             st.apply_toplevel_tiled_hint(s);
         });
-        top.send_configure();
+        // Dedup: emitir configure SOLO si el estado pendiente difiere del último
+        // enviado. Un cliente que ya está en fullscreen y vuelve a pedir
+        // `set_fullscreen` (Chromium/Electron lo hacen al cambiar de vídeo o de
+        // pestaña; ver el early-return de `ClientRequest` en
+        // `enter_xdg_fullscreen_inner`) recibía un configure IDÉNTICO con el estado
+        // Fullscreen puesto de nuevo. El navegador lo lee como "acabas de entrar en
+        // pantalla completa" y re-muestra el toast "Pulsa Esc para salir", que por
+        // eso no se va nunca.
+        //
+        // `send_pending_configure()` es el dedup propio de Smithay: no-op cuando el
+        // estado pendiente es igual al ya enviado. Mismo patrón que ya aplica
+        // `compositor/overlap/system/resolve.rs`, donde el configure incondicional
+        // inundaba al cliente en cada tick de mantenimiento.
+        top.send_pending_configure();
         break;
     }
 }
diff --git a/crates/halley-wl/src/frame_loop/activity.rs b/crates/halley-wl/src/frame_loop/activity.rs
index 41ce56c..0644a50 100644
--- a/crates/halley-wl/src/frame_loop/activity.rs
+++ b/crates/halley-wl/src/frame_loop/activity.rs
@@ -2,6 +2,7 @@ use std::time::Instant;
 
 use crate::compositor::root::Halley;
 use crate::compositor::screenshot;
+use smithay::input::pointer::CursorImageStatus;
 
 #[derive(Clone, Copy, Debug, Default)]
 pub(crate) struct TtyOutputAnimationRedrawState {
@@ -280,6 +281,14 @@ pub(crate) fn tty_output_animation_redraw_state(
         || spawn_activation_active
         || fullscreen_motion_active
         || maximize_motion_active;
+    // Cursor themed animado: forzar redraw del output para que el blit del cursor
+    // avance de frame por elapsed (no hay timer dedicado). Reutiliza el mismo
+    // mecanismo que el fondo animado. El cache del sprite se resuelve en el
+    // primer render (al mover el ratón); tras eso este flag queda true y el
+    // scheduler mantiene redraws continuos mientras el cursor animado esté activo.
+    let cursor_named_animation_active =
+        matches!(st.platform.cursor_manager.cursor_image(), CursorImageStatus::Named(_))
+            && st.platform.cursor_manager.named_cursor_is_animated();
     let active = fade_related
         || cluster_tile_active
         || close_window_active
@@ -289,7 +298,8 @@ pub(crate) fn tty_output_animation_redraw_state(
         || viewport_pan_active
         || camera_smoothing_active
         || overlay_active
-        || background_active;
+        || background_active
+        || cursor_named_animation_active;
 
     TtyOutputAnimationRedrawState {
         active,
diff --git a/crates/halley-wl/src/input/pointer/focus/surface.rs b/crates/halley-wl/src/input/pointer/focus/surface.rs
index 6f6e443..c156b34 100644
--- a/crates/halley-wl/src/input/pointer/focus/surface.rs
+++ b/crates/halley-wl/src/input/pointer/focus/surface.rs
@@ -252,11 +252,32 @@ pub(crate) fn layer_surface_focus_for_screen(
 )> {
     let block_non_overlay =
         fullscreen_hit_blocks_non_overlay_layers(st, ws_w, ws_h, sx, sy, now, resize_preview);
+    // Un fullscreen asentado TAPA las capas Top/Overlay: el frame builder se salta
+    // su dibujo (`suppress_top_overlay_layers` en render/frame/scene.rs). Si el
+    // hit-test no las salta también, la barra de Noctalia, las notificaciones y el
+    // click-shield quedan INVISIBLES PERO CLICABLES: el usuario clica "dentro" del
+    // navegador a pantalla completa y el clic se lo come una capa que no se ve.
+    //
+    // Se usa el MISMO predicado que el render (`current_monitor_has_settled_
+    // fullscreen`) y no `block_non_overlay`, porque ese depende de que el puntero
+    // ACIERTE el nodo fullscreen (`pick_hit_node_at`): sobre una zona cubierta por
+    // una capa suprimida el hit-test del nodo falla y el bloqueo no se activaba
+    // justo donde hace falta.
+    let suppress_top_overlay =
+        crate::compositor::fullscreen::system::current_monitor_has_settled_fullscreen(st, now);
 
     let mut placements = crate::compositor::monitor::layer_shell::layer_shell_placements(
         st,
         (ws_w.max(1), ws_h.max(1)).into(),
     );
+    // `layer_shell_placements` returns surfaces ascending by (layer, reservation,
+    // mapping order), so the topmost within-layer surface is last. Reverse first,
+    // then the stable layer sort preserves that within-layer descending order —
+    // giving a true topmost-first hit test. Without this, a fullscreen surface
+    // mapped *before* a panel on the same layer (e.g. Noctalia's click-shield)
+    // wins the hit test over the panel above it, so clicks inside the panel are
+    // delivered to the shield and dismiss the panel.
+    placements.reverse();
     placements.sort_by_key(|placement| {
         std::cmp::Reverse(match placement.layer {
             smithay::wayland::shell::wlr_layer::Layer::Background => 0u8,
@@ -267,6 +288,16 @@ pub(crate) fn layer_surface_focus_for_screen(
     });
 
     for placement in placements {
+        // Capas suprimidas por el render: también invisibles para el hit-test.
+        if suppress_top_overlay
+            && matches!(
+                placement.layer,
+                smithay::wayland::shell::wlr_layer::Layer::Top
+                    | smithay::wayland::shell::wlr_layer::Layer::Overlay
+            )
+        {
+            continue;
+        }
         if block_non_overlay
             && !matches!(
                 placement.layer,
diff --git a/crates/halley-wl/src/portal/mod.rs b/crates/halley-wl/src/portal/mod.rs
index 1e49507..e02cf64 100644
--- a/crates/halley-wl/src/portal/mod.rs
+++ b/crates/halley-wl/src/portal/mod.rs
@@ -674,6 +674,8 @@ fn resolve_cursor_meta_for_region(
             crate::render::themed_cursor_sprite_with_fallback(cursor_config, CursorIcon::Default)
         })?;
 
+    // Snapshot estática del cursor para el metadata de screencast: usamos el
+    // primer frame (no animamos aquí — el cliente recibe una imagen fija).
     Some(CursorMetaFrame {
         pos_x,
         pos_y,
@@ -681,7 +683,7 @@ fn resolve_cursor_meta_for_region(
         hotspot_y: sprite.hotspot_y,
         width: sprite.width as u32,
         height: sprite.height as u32,
-        bgra: sprite.pixels_bgra.clone(),
+        bgra: sprite.first_frame().pixels_bgra.clone(),
     })
 }
 
diff --git a/crates/halley-wl/src/render/cursor.rs b/crates/halley-wl/src/render/cursor.rs
index 813c6a6..95aefc8 100644
--- a/crates/halley-wl/src/render/cursor.rs
+++ b/crates/halley-wl/src/render/cursor.rs
@@ -79,15 +79,21 @@ fn surface_tree_root(surface: &WlSurface) -> WlSurface {
 ///
 /// Coordinates are in the same screen-space used for hit-testing so the
 /// rendered cursor stays aligned with pointer events on every backend.
+///
+/// `elapsed_ms` es el tiempo transcurrido desde que empezó la animación del
+/// cursor activo; se usa para seleccionar el frame de un cursor animado.
 pub(crate) fn draw_cursor_sprite<F: Frame>(
     frame: &mut F,
     damage: Rectangle<i32, Physical>,
     cursor_screen: (f32, f32),
     sprite: &SoftwareCursorSprite,
+    elapsed_ms: u64,
 ) -> Result<(), F::Error> {
     let (sx, sy) = cursor_screen;
     let x0 = sx.round() as i32 - sprite.hotspot_x;
     let y0 = sy.round() as i32 - sprite.hotspot_y;
+    let frame_data = sprite.frame_at(elapsed_ms);
+    let pixels = &frame_data.pixels_bgra;
     let w = sprite.width;
     let h = sprite.height;
 
@@ -95,24 +101,24 @@ pub(crate) fn draw_cursor_sprite<F: Frame>(
         let mut x = 0usize;
         while x < w {
             let base = (y * w + x) * 4;
-            let a = sprite.pixels_bgra[base + 3];
+            let a = pixels[base + 3];
             if a == 0 {
                 x += 1;
                 continue;
             }
 
-            let b = sprite.pixels_bgra[base];
-            let g = sprite.pixels_bgra[base + 1];
-            let r = sprite.pixels_bgra[base + 2];
+            let b = pixels[base];
+            let g = pixels[base + 1];
+            let r = pixels[base + 2];
 
             // Merge identical neighbouring pixels into a single rect call.
             let mut run_end = x + 1;
             while run_end < w {
                 let i = (y * w + run_end) * 4;
-                if sprite.pixels_bgra[i] != b
-                    || sprite.pixels_bgra[i + 1] != g
-                    || sprite.pixels_bgra[i + 2] != r
-                    || sprite.pixels_bgra[i + 3] != a
+                if pixels[i] != b
+                    || pixels[i + 1] != g
+                    || pixels[i + 2] != r
+                    || pixels[i + 3] != a
                 {
                     break;
                 }
diff --git a/crates/halley-wl/src/render/cursor_theme.rs b/crates/halley-wl/src/render/cursor_theme.rs
index e4f0e1a..80b74e0 100644
--- a/crates/halley-wl/src/render/cursor_theme.rs
+++ b/crates/halley-wl/src/render/cursor_theme.rs
@@ -1,6 +1,7 @@
 use std::collections::HashMap;
 use std::fs;
 use std::sync::{Arc, Mutex};
+use std::time::Instant;
 
 use halley_config::CursorConfig;
 use once_cell::sync::Lazy;
@@ -9,16 +10,49 @@ use smithay::utils::IsAlive;
 use xcursor::{CursorTheme, parser::Image};
 
 // ---------------------------------------------------------------------------
-// Sprite data
+// Sprite data (soporte multi-frame para cursores XCursor animados)
 // ---------------------------------------------------------------------------
 
+/// Un único frame de un cursor animado.
+#[derive(Clone)]
+pub(crate) struct FrameData {
+    pub(crate) pixels_bgra: Vec<u8>,
+    pub(crate) delay_ms: u32,
+}
+
 #[derive(Clone)]
 pub(crate) struct SoftwareCursorSprite {
     pub(crate) width: usize,
     pub(crate) height: usize,
     pub(crate) hotspot_x: i32,
     pub(crate) hotspot_y: i32,
-    pub(crate) pixels_bgra: Vec<u8>,
+    pub(crate) frames: Vec<FrameData>,
+}
+
+impl SoftwareCursorSprite {
+    /// Devuelve el frame correspondiente al tiempo transcurrido (ms).
+    /// Para sprites de un solo frame (cursores estáticos) evita cálculos.
+    pub(crate) fn frame_at(&self, elapsed_ms: u64) -> &FrameData {
+        if self.frames.len() == 1 {
+            return &self.frames[0];
+        }
+        let cycle_ms: u64 = self.frames.iter().map(|f| f.delay_ms as u64).sum();
+        let pos = elapsed_ms % cycle_ms.max(1);
+        let mut acc = 0u64;
+        for frame in &self.frames {
+            acc += frame.delay_ms as u64;
+            if pos < acc {
+                return frame;
+            }
+        }
+        // Fallback (no debería alcanzarse)
+        &self.frames[0]
+    }
+
+    /// Acceso rápido al primer frame (para paths que no necesitan animación).
+    pub(crate) fn first_frame(&self) -> &FrameData {
+        &self.frames[0]
+    }
 }
 
 // ---------------------------------------------------------------------------
@@ -76,9 +110,31 @@ impl CursorSpriteManager {
 static CURSOR_SPRITES: Lazy<Mutex<CursorSpriteManager>> =
     Lazy::new(|| Mutex::new(CursorSpriteManager::default()));
 
+// ---------------------------------------------------------------------------
+// Estado de animación del cursor
+// ---------------------------------------------------------------------------
+
+pub(crate) struct CursorAnimState {
+    started_at: Option<Instant>,
+    /// Duración total del ciclo en ms (0 == cursor estático).
+    cycle_ms: u64,
+}
+
+impl Default for CursorAnimState {
+    fn default() -> Self {
+        Self {
+            started_at: None,
+            cycle_ms: 0,
+        }
+    }
+}
+
 pub(crate) struct CursorManager {
     current_cursor: CursorImageStatus,
     sprites: CursorSpriteManager,
+    anim: CursorAnimState,
+    /// Último icono named resuelto, para detectar cambios y resetear la animación.
+    last_named_icon: Option<CursorIcon>,
 }
 
 impl Default for CursorManager {
@@ -86,6 +142,8 @@ impl Default for CursorManager {
         Self {
             current_cursor: CursorImageStatus::default_named(),
             sprites: CursorSpriteManager::default(),
+            anim: CursorAnimState::default(),
+            last_named_icon: None,
         }
     }
 }
@@ -96,6 +154,11 @@ impl CursorManager {
     }
 
     pub(crate) fn set_cursor_image(&mut self, cursor: CursorImageStatus) {
+        // Si dejamos de tener un cursor named, limpiamos el tracking para que
+        // la próxima vez que se resuelva un Named se resetee la animación.
+        if !matches!(cursor, CursorImageStatus::Named(_)) {
+            self.last_named_icon = None;
+        }
         self.current_cursor = cursor;
     }
 
@@ -104,15 +167,43 @@ impl CursorManager {
             && !surface.alive()
         {
             self.current_cursor = CursorImageStatus::default_named();
+            // Al caer a default_named el próximo render resolverá Default y
+            // reseteará la animación en sprite_with_fallback.
         }
     }
 
+    /// Tiempo transcurrido desde que empezó la animación actual (ms).
+    pub(crate) fn cursor_elapsed_ms(&self) -> u64 {
+        self.anim.started_at.map(|t| t.elapsed().as_millis() as u64).unwrap_or(0)
+    }
+
+    /// ¿El cursor Named actual es animado (multi-frame)? Sólo lectura: NO resetea
+    /// la animación. Se basa en `cycle_ms` que `sprite_with_fallback` puebla la
+    /// primera vez que se resuelve un icono Named con frames animados (valor > 0).
+    /// Esto evita el deadlock de mirar el cache (que aún podría estar vacío en
+    /// el primer tick), porque `cycle_ms` queda fijado POR el primer render.
+    pub(crate) fn named_cursor_is_animated(&self) -> bool {
+        matches!(self.current_cursor, CursorImageStatus::Named(_)) && self.anim.cycle_ms > 0
+    }
+
     pub(crate) fn sprite_with_fallback(
         &mut self,
         cursor: &CursorConfig,
         icon: CursorIcon,
     ) -> Option<Arc<SoftwareCursorSprite>> {
-        self.sprites.sprite_with_fallback(cursor, icon)
+        let sprite = self.sprites.sprite_with_fallback(cursor, icon)?;
+        // Resetear la animación solo cuando el icono named cambia de verdad,
+        // no en cada frame de render.
+        if self.last_named_icon != Some(icon) {
+            self.last_named_icon = Some(icon);
+            self.anim.started_at = Some(Instant::now());
+            self.anim.cycle_ms = if sprite.frames.len() <= 1 {
+                0
+            } else {
+                sprite.frames.iter().map(|f| f.delay_ms as u64).sum()
+            };
+        }
+        Some(sprite)
     }
 }
 
@@ -120,12 +211,24 @@ impl CursorManager {
 // Theme loading
 // ---------------------------------------------------------------------------
 
-fn pick_best_cursor_image(images: &[Image], requested_size: u32) -> Option<&Image> {
-    images.iter().min_by_key(|img| {
-        let nominal_delta = img.size.abs_diff(requested_size);
-        let width_delta = img.width.abs_diff(requested_size);
-        (nominal_delta, width_delta, img.delay)
-    })
+/// Elige el tamaño objetivo con el mismo criterio que la versión anterior
+/// (menor diferencia de size nominal y ancho real; delay solo desempata) y
+/// devuelve TODOS los frames de ese tamaño, en orden de archivo.
+fn pick_best_size_frames(images: &[Image], requested_size: u32) -> Vec<Image> {
+    let target = images
+        .iter()
+        .min_by_key(|img| {
+            let nominal_delta = img.size.abs_diff(requested_size);
+            let width_delta = img.width.abs_diff(requested_size);
+            (nominal_delta, width_delta, img.delay)
+        })
+        .map(|img| img.size)
+        .unwrap_or(requested_size);
+    images
+        .iter()
+        .filter(|img| img.size == target && img.width == target)
+        .cloned()
+        .collect()
 }
 
 fn load_cursor_from_theme(
@@ -144,19 +247,36 @@ fn load_cursor_from_theme(
         let Some(images) = xcursor::parser::parse_xcursor(&bytes) else {
             continue;
         };
-        let Some(image) = pick_best_cursor_image(&images, requested_size) else {
+        let frames = pick_best_size_frames(&images, requested_size);
+        if frames.is_empty() {
             continue;
-        };
-        let width = usize::try_from(image.width).ok()?;
-        let height = usize::try_from(image.height).ok()?;
+        }
+        // Todos los frames del tamaño elegido comparten dimensiones y hotspot.
+        // Extraemos todos los datos del primer frame ANTES de mover `frames`
+        // (el `.into_iter()` consume el Vec y rompería el borrow de `first`).
+        let first = &frames[0];
+        let width = usize::try_from(first.width).ok()?;
+        let height = usize::try_from(first.height).ok()?;
         let max_hotspot_x = i32::try_from(width.saturating_sub(1)).ok().unwrap_or(0);
         let max_hotspot_y = i32::try_from(height.saturating_sub(1)).ok().unwrap_or(0);
+        let hotspot_x = (first.xhot as i32).clamp(0, max_hotspot_x);
+        let hotspot_y = (first.yhot as i32).clamp(0, max_hotspot_y);
+        let frame_data: Vec<FrameData> = frames
+            .into_iter()
+            .map(|img| FrameData {
+                // Mismos bytes que antes: el campo se llama bgra pero upstream
+                // copia pixels_rgba tal cual (sin swap R<->B). Respetamos la
+                // semántica existente para no introducir regresión visual.
+                pixels_bgra: img.pixels_rgba,
+                delay_ms: img.delay,
+            })
+            .collect();
         return Some(Arc::new(SoftwareCursorSprite {
             width,
             height,
-            hotspot_x: (image.xhot as i32).clamp(0, max_hotspot_x),
-            hotspot_y: (image.yhot as i32).clamp(0, max_hotspot_y),
-            pixels_bgra: image.pixels_rgba.clone(),
+            hotspot_x,
+            hotspot_y,
+            frames: frame_data,
         }));
     }
     None
diff --git a/crates/halley-wl/src/render/frame/draw.rs b/crates/halley-wl/src/render/frame/draw.rs
index a99976d..87f24cd 100644
--- a/crates/halley-wl/src/render/frame/draw.rs
+++ b/crates/halley-wl/src/render/frame/draw.rs
@@ -448,12 +448,17 @@ pub(super) fn draw_scene_windows_and_hud(
         },
     )?;
 
-    draw_layer_groups(
-        frame,
-        prepared.damage,
-        &scene.layer_top_elements,
-        blur_ctx.as_deref_mut(),
-    )?;
+    // A settled fullscreen app covers the Top/Overlay layer-shell surfaces (bar,
+    // notifications, launcher) — Halley has no dedicated fullscreen render route, so
+    // without this the layers, drawn after the window stack, always sit on top.
+    if !scene.suppress_top_overlay_layers {
+        draw_layer_groups(
+            frame,
+            prepared.damage,
+            &scene.layer_top_elements,
+            blur_ctx.as_deref_mut(),
+        )?;
+    }
 
     if !scene.fullscreen_active_elements.is_empty() {
         let _ = draw_render_elements(
@@ -522,12 +527,14 @@ pub(super) fn draw_scene_windows_and_hud(
 
     draw_pin_badges(frame, st, &scene.pin_badges, prepared.damage)?;
 
-    draw_layer_groups(
-        frame,
-        prepared.damage,
-        &scene.layer_overlay_elements,
-        blur_ctx.as_deref_mut(),
-    )?;
+    if !scene.suppress_top_overlay_layers {
+        draw_layer_groups(
+            frame,
+            prepared.damage,
+            &scene.layer_overlay_elements,
+            blur_ctx.as_deref_mut(),
+        )?;
+    }
 
     if st.should_draw_focus_ring_preview(prepared.now) {
         let focus_ring = st.active_focus_ring();
@@ -1249,7 +1256,13 @@ pub(super) fn draw_cursor_layer(
             smithay::input::pointer::CursorImageStatus::Hidden => false,
             smithay::input::pointer::CursorImageStatus::Named(icon) => {
                 if let Some(sprite) = cursor_manager.sprite_with_fallback(cursor_config, *icon) {
-                    draw_cursor_sprite(frame, damage, (sx, sy), sprite.as_ref())?;
+                    draw_cursor_sprite(
+                        frame,
+                        damage,
+                        (sx, sy),
+                        sprite.as_ref(),
+                        cursor_manager.cursor_elapsed_ms(),
+                    )?;
                     false
                 } else {
                     true
diff --git a/crates/halley-wl/src/render/frame/scene.rs b/crates/halley-wl/src/render/frame/scene.rs
index d4506a4..c31d6c4 100644
--- a/crates/halley-wl/src/render/frame/scene.rs
+++ b/crates/halley-wl/src/render/frame/scene.rs
@@ -111,6 +111,10 @@ pub(super) struct SceneCollections {
     pub(super) render_nodes: Vec<NodeSnapshot>,
     pub(super) bearing_layouts: Vec<BearingChipLayout>,
     pub(super) blur_rects: Vec<BlurRect>,
+    /// When a fullscreen app is settled on the render monitor, the Top/Overlay
+    /// layer-shell surfaces (bar, notifications, launcher) are skipped so the
+    /// fullscreen covers them. See `current_monitor_has_settled_fullscreen`.
+    pub(super) suppress_top_overlay_layers: bool,
 }
 
 pub(super) struct CursorScene {
@@ -165,6 +169,8 @@ pub(super) fn collect_debug_frame_scene(
                 .collect();
         return SceneCollections {
             session_lock_elements,
+            // Pantalla bloqueada: no hay fullscreen visible, no suprimir capas top.
+            suppress_top_overlay_layers: false,
             layer_background_elements: Vec::new(),
             layer_bottom_elements: Vec::new(),
             layer_top_elements: Vec::new(),
@@ -355,6 +361,8 @@ pub(super) fn collect_debug_frame_scene(
         render_nodes,
         bearing_layouts,
         blur_rects,
+        suppress_top_overlay_layers:
+            crate::compositor::fullscreen::system::current_monitor_has_settled_fullscreen(st, now),
     }
 }
 
